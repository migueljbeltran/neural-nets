# neural-nets

renamed code to src bc having a code/ folder breaks import torch on python 3.14

## data

stage 2+ datasets are too big for github so theyre gitignored. download them from canvas and drop them in:

- data/stage_2_data/ — train.csv, test.csv
- data/stage_3_data/, stage_4_data/, stage_5_data/ — when needed
